# Sections Folder

This folder is here for future growth.

The first starter version keeps the portfolio simple by placing the visible sections directly inside `index.html`.

Later, if you want, you can use this folder to move section content into separate files.
